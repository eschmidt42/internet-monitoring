[project]
name = "mqtt-bridge"
version = "0.1.0"
description = "A MQTT bridge"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "Flask==3.1.3",
    "paho-mqtt==2.1.0",
    "gunicorn==23.0.0"
]
